[project]
name = "dime-api"
version = "1.0.0"
description = "DiME - Diagnóstico de Métricas de Vendedores"
requires-python = ">=3.14"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.backends._legacy:_Backend"
